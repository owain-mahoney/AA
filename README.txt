Start if you do not have postgres SQL database plase do this first 

too retrive our database for use a live version must be on the machine your using to access it in the repository 
you will need to restore a backup file saved in the repository 

further help if needed:
using pgadmin from now on if using a differnt interface seek help from IT department 
to restore database create a new database name it postgres or easy but can use any name 
1. save download our payroll.backup file in the git repository
2. right click on the database you created in pgadmin 
2. click the restore option 
3. choose the backup file you downloaded from git 
4.click restore and wait 

might take a few minutes when complete fresh the database in the right click display menu 
and see if all the tables are there
